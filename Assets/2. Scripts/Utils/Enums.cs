namespace Scripts.Utils
{
    public enum TileType
    {
        Normal,
        Obstacle,
        Unwalkable
    }
    
    public enum GameState
    {
        MainMenu,
        UnitPlacement,
        Playing,
        Paused,
        GameOver
    }
}